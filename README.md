# Proyecto Hora Japón - React + Vite + TailwindCSS

## Instalación y puesta en marcha

### 1. Instalar Node.js y npm

- Descarga e instala la versión LTS de Node.js desde [https://nodejs.org](https://nodejs.org)
- Verifica la instalación:
  ```bash
  node -v
  npm -v

npm create vite@latest hora-japon -- --template react
cd hora-japon
npm install


npm create vite@latest hora-japon -- --template react
cd hora-japon
npm install


npm uninstall tailwindcss
npm install -D tailwindcss@3.4.1 postcss autoprefixer


npx tailwindcss init -p


#Como funciona generacion markers
Al crear un archivo .json en /public/markers y hacer build, se ejecuta el script que genera el index.json.

POLLA NEGRA